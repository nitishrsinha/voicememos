{
  "name": "voicememos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create voicememos-db",
    "db:migrate:local": "wrangler d1 migrations apply voicememos-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply voicememos-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
